"use client"

import { observer } from "mobx-react-lite"
import { FC, useState, useEffect } from "react"

const HomePage = observer(() => {
  return <div>tinder remote</div>
})

export default HomePage
